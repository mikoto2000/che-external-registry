# che-external-registry-buildkit

## Build:

```sh
docker build -t mikoto2000/che-external-registry-buildkit:latest .
```

## Usage:

```sh
docker build -t mikoto2000/che-external-registry-buildkit:latest .
```
